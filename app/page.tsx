import HomeModeSelect from "@/components/HomeModeSelect";

/** Home is only the Cards vs Questions picker. Content.tsx is unused here. */
export default function Home() {
  return (
    <main>
      <HomeModeSelect />
    </main>
  );
}
